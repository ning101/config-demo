server:
  port: 8080

spring:
  profiles: dev
  application:
    name: config-zuul
  cloud:
    config:
      uri: http://localhost:8081/
      label: master
      profile: dev
#      discovery:
#        enabled: true
#        service-id: config-server