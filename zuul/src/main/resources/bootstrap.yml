spring:
  profiles: dev
  application:
    name: config-zuul-client
  cloud:
    config:
      uri: http://localhost:8081
      label: master
      profile: dev
#      discovery:
#        enabled: true
#        service-id: config-server