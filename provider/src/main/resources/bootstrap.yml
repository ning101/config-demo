server:
  port: 8082

eureka:
  client:
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://localhost:8081/eureka/,http://localhost:9081/eureka

spring:
  application:
    name: provider

info:
  name: provider
  server:
    port: ${server.port}
